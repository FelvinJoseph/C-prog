#include<stdio.h>
#include<string.h>
int main()
{
   char s[]="Hello world";
   int length=strlen(s);
   char reversed[length];
   for(int i=0;i<length;i++)
   {
      reversed[i]=s[length-i-1];
   }
   reversed[length]='\0';
   printf("Reversed string is : %s \n",reversed);
   return 0;
}
