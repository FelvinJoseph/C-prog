#include<stdio.h>
void main()
{
    int num1,num2,i,j,large;
    printf("enter the numbers: ");
    scanf("%d%d",&num1,&num2);
    j=(num1<num2)?num1:num2;
    for(i=1;i<=j;i++)
    {
        
        if(num1%i==0&&num2%i==0)
        large=i;
    }
    printf("hcf is %d",large);
}
